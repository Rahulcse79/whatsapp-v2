package fixtures

// VIOLATION FIXTURE - never compiled. Rule 1 must reject this.
import android.content.Context
import androidx.lifecycle.ViewModel

class AndroidInDomain(private val context: Context)
