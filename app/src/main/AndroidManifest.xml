<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      Declared here, modelled in AppPermission, and requested in context by
      PermissionCoordinator. Each one has a rationale and a defined behaviour when it is
      refused - see AppPermission.deniedExplanation.

      The foreground-service and Telecom entries arrive with the tasks that need them
      (28, 34, 51); declaring them before anything starts a service would be asking for
      capability the app cannot yet use. Task 51 added the camera and microphone service
      types, which is the last of them.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        android:minSdkVersion="33" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_CONNECT"
        android:minSdkVersion="31" />

    <!--
      Staying reachable when the app is not on screen. RECEIVE_BOOT_COMPLETED brings the
      registrations back after a reboot; REQUEST_IGNORE_BATTERY_OPTIMIZATIONS lets the app
      ask (once, and the user may say no) to be left out of the background restrictions that
      would otherwise end the registration service - and, on Android 12+, it is also what
      permits the service to be restarted from an alarm after the task is swiped away.
    -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
      An incoming call must reach the lock screen (Task 37). From Android 14 this is
      granted only to apps whose core function is calling, which this app's is; a device
      that withholds it still shows the heads-up notification, which stays answerable.
    -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!--
      The proximity screen-off lock, held only while a call is on the earpiece (Task 40).
      No wake lock is held while unregistered (§6) - ProximityLock releases on every route
      that is not the earpiece and on the end of every call.
    -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      Vibrating for an incoming call, and only when the ringer says to (Task 37).
      RingerPolicy decides; without this permission the buzz silently does nothing, which
      is the kind of failure nobody notices until a call is missed in a meeting.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!--
      Foreground service. The types match what the service is actually doing: specialUse
      while only holding a registration - which is not a phone call, a data sync, or any
      other standard type - phoneCall once a call is in progress, and microphone/camera
      while a call is using them. Declaring registration as a phone call would be a false
      claim about behaviour.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
      Video calling (Task 51). From Android 14 a foreground service must declare what it
      is doing and the platform checks each declared type against its permission at the
      moment startForeground runs - so declaring camera here is only half of it.
      ForegroundServiceTypes picks the subset that is actually true and permitted, which
      is what stops a user who declined the camera from getting a crash instead of an
      audio call.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!--
      Video calling is optional: the app must install on devices without a camera, and a
      user who declines CAMERA keeps a working phone - the call is placed without video
      rather than refused (Task 51, MediaProfile.downgradedWhenCameraUnavailable).
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <!--
      Deliberately minimal. Permissions, the foreground service and its types, the
      Telecom ConnectionService and CallStyle notifications are declared by the tasks
      that introduce them (15, 28, 34, 37, 51).

      android:allowBackup is false from the first commit because this app stores SIP
      credentials, and backup would copy them off the device (§7, DoD 12).

      usesCleartextTraffic and networkSecurityConfig are Task 62's platform half. They do
      not cover SIP over TLS - PJSIP opens its own sockets and runs its own TLS through
      the OpenSSL that build-pjsip.yml cross-compiles - so the settings for that path are
      the ones RealPjsipCoreGateway gives its TLS transport. Both are needed; see
      docs/security.md.
    -->
    <application
        android:name=".SipApplication"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WhatsAppV2">

        <!--
          stopWithTask=false is the default and is spelled out because the whole design
          leans on it: the service must outlive the task, and it is onTaskRemoved - only
          delivered when this is false - that books the restart on the builds that kill the
          process anyway.
        -->
        <service
            android:name=".service.RegistrationService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="phoneCall|specialUse|camera|microphone">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Maintains a SIP registration so incoming calls can be received while the app is in the background." />
        </service>

        <!--
          Telecom's entry point (Task 34, §3). The permission is what tells the platform
          only Telecom may bind this, and Telecom refuses a ConnectionService that does
          not declare it - the service is simply never called and calls never appear.
        -->
        <service
            android:name=".telecom.SipConnectionService"
            android:exported="true"
            android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE">
            <intent-filter>
                <action android:name="android.telecom.ConnectionService" />
            </intent-filter>
        </service>

        <!--
          The call screen (Tasks 37, 39). Its own task and its own activity because a
          full-screen intent needs one, and because it must show over the keyguard without
          dragging the rest of the app onto the lock screen with it.

          singleTask: one call screen, whichever of the three ways opened it - the dialler,
          a notification, or the platform's own answer button.

          excludeFromRecents: a finished call has no screen worth returning to, and leaving
          one in the recents list invites someone to reopen a call that is over.
        -->
        <activity
            android:name=".call.CallActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"
            android:taskAffinity=".call"
            android:theme="@style/Theme.WhatsAppV2" />

        <!--
          The notification's answer and decline buttons. Not exported: nothing outside this
          app may end or answer a call.
        -->
        <receiver
            android:name=".call.CallActionReceiver"
            android:exported="false" />

        <!--
          Registrations after a reboot or an update: the process the platform starts to
          deliver these restores every logged-in account, and the receiver starts the
          service that keeps it alive. Exported because the sender is the system, and
          limited to the system by the intent filter - nobody else can send BOOT_COMPLETED.
        -->
        <receiver
            android:name=".boot.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- The restart alarm's target after the task is swiped away; see RegistrationService. -->
        <receiver
            android:name=".service.ServiceRestartReceiver"
            android:exported="false" />

        <!--
          The push wake path (Task 38, ADR-004). Fires only when Firebase is configured for
          the build; with no google-services.json the SDK never delivers a message and this
          service is simply never called, which is the honest behaviour for a deployment
          that has no push gateway yet.
        -->
        <service
            android:name=".push.SipMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!--
          singleTop, so the call screen's "Add call" brings this activity back and hands
          it a destination through onNewIntent rather than stacking a second copy of the
          whole app on top of the first.
        -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
