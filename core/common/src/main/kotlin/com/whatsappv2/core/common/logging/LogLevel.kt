package com.whatsappv2.core.common.logging

/** Severity ordering used by [Logger]. Declaration order is the comparison order. */
enum class LogLevel {
    VERBOSE,
    DEBUG,
    INFO,
    WARN,
    ERROR,
}
