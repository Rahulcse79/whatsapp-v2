Paste this whole file as the opening message of a fresh Claude Code session in this repo.

Repo: /Users/rahulsingh/Desktop/whatsapp-v2   Branch: docs/native-mandate-design   (34+ commits ahead of origin, NOTHING pushed — never push without being asked)
Read first: docs/master-engineering-prompt.md (how to work here), docs/HANDOFF.md §0b + §0a (what is verified on hardware), then this file.
Memory dir: ~/.claude/projects/-Users-rahulsingh-Desktop-whatsapp-v2/memory/ — MEMORY.md indexes it; tc15-device-driving-and-server-quirks.md and lyra-closure-facts.md are the ones that matter today.

WHAT EXISTS (all verified on two Zebra TC15s on 2026-09-11, commits 2763f778 → 9a066a10):
- Video calls (send, preview, PLI repair, orientation, flip, video off/on, hold/resume), blind + attended transfer, conference (mod_conference 3000, DTMF, hold), call waiting, recording, incoming with screen off / app in background / app in front, Wi-Fi drop mid-call, logout/login, restart auto-register.
- Lyra is PJSIP's native codec compiled into libpjsua2.so (config_site.h PJMEDIA_HAS_LYRA_CODEC 1, liblyra.a from the vendored closure). A 22-minute APK↔APK Lyra call ran at 3.1 kbit/s, 0 loss, media direct phone-to-phone.
- Last commit 9a066a10 = the automated feature sweep: 6 defects found and fixed (process crash on "video off", recording wrote nothing, full-screen intent never fired, call-waiting screen wrong, spurious registration failures on Wi-Fi blips, hangup error log). JVM: 1186 tests, detekt clean.

TEST SETUP (do not re-derive):
- Phone A: TC15 serial 24110524701351, IP 192.168.0.108, accounts 7000@192.168.80.145 + "localfs" 1001@192.168.0.101 (lyra, PCMU, opus).
- Phone B: TC15 serial 24143524701316, IP 192.168.0.117, account 1002@192.168.0.101 (lyra, PCMU, opus; Rahul deleted 7004 on it). ONE USB cable — `adb devices` first; only the plugged phone is driveable, Rahul answers the other.
- adb: /Users/rahulsingh/Downloads/platform-tools/adb. Drive the UI with `uiautomator dump` bounds (helper scripts existed in the old session's scratchpad — rewrite: nodes(), tapd(desc), tapt(text); after `input text` press KEYCODE_BACK once to drop the keyboard, and the first tap after that is often swallowed; Back on the call screen CLOSES it — reopen via `cmd statusbar expand-notifications` → "Ongoing call"; close the keypad with the "Hide the keypad" button, never Back).
- Local FreeSWITCH on the Mac: start `/usr/local/freeswitch/bin/freeswitch -nc -nonat`; binds en0 (192.168.0.101 on office Wi-Fi). fs_cli at /usr/local/freeswitch/bin/fs_cli; log /usr/local/freeswitch/var/log/freeswitch/freeswitch.log. User password 1234. Dialplan (outside the repo): default/03_whatsapp_v2_test_apps.xml → 9196 echo (audio+video), 9197 bridge to loopback/9196 (transferable), 9198 tone, 9199 unrouted, 3000 conference; default/00_whatsapp_v2_bypass.xml → bypass_media for 1001↔1002 (needed for Lyra; FreeSWITCH does not know Lyra and 488s a Lyra-only offer).
- Inbound calls to a phone without the other phone: `fs_cli -x "bgapi originate {origination_caller_id_number=9196,origination_caller_id_name=Echo,absolute_codec_string='PCMU'}user/1002 &echo"` (drop absolute_codec_string to get an m=video offer). Second member into the conference: `originate loopback/9196/default &conference(3000)`.
- FreeSWITCH answers EVERY blind REFER with 200 — the transfer-refused path cannot be produced on it.
- Build: `SWIG_LIB=$HOME/.local/share/swig/4.4.1 ./build.sh --install` (full native ~5 min; `--reuse-native` for Kotlin-only changes, ~1 min). MacPorts swig lacks Java typemaps — `sudo port install swig-java` is the proper fix. Tests: `./gradlew testDebugUnitTest test detekt -x :pjsip:api:generatePjsua2Bindings -x :pjsip:buildPjsua2Native`. Native-crash check after any device test: `adb logcat -d | grep -E "Fatal signal|FATAL EXCEPTION|Assert failed"` and compare `pidof com.whatsappv2` before/after.
- detekt LargeClass caps PjsipSipEngine and RealPjsipCoreGateway — new pure logic goes to file level or a new file.

STILL OWED (in order):
1. Update docs/HANDOFF.md with a §0c for the sweep (commit 9a066a10 message has everything) and delete this file when done.
2. On the OFFICE server 192.168.80.145 (7000 ↔ 7004, Zoiper 7001): audio heard, video both ways, and the transfer-REFUSED path (a phone that declines the REFER target → 486 → TransferFailed → call returns to Connected). The office extensions answered 480 all of 2026-09-11 morning; Rahul registers them.
3. Recording start failure is not surfaced: SipRecordingGateway.startRecording is fire-and-forget, so a stack refusal would still show "Recording this call". Consider reporting the failure back (PjsipCallRecorder) — low risk now that the .wav name is right.
4. "CallAudioCoordinator: Audio focus was not granted" is logged as WARN on every resume/second call: Telecom already holds the call's focus for a self-managed connection. Downgrade to info with the reason, or stop requesting when Telecom holds it.
5. JoinConferenceUseCase has no UI entry point and the real gateway never emits a roster (no conference-info subscription) — a conference is a plain call to the bridge by design (ADR-003). Decide whether a "Join conference" affordance is wanted.
6. Lyra costs ~120 % of one CPU core on the TC15 (TFLite encode+decode). Not a defect; worth a line in docs/architecture.md ADR-008.
7. Push the branch when Rahul says so; CI then builds armeabi-v7a/x86_64 and exercises the Lyra prefix cache for the first time.

HOW TO REPORT: "compiles", "registered", "connected on the wire", "heard" are four different claims — say which one you have. Every hardware claim needs a log line or a screenshot behind it. Commit small, with the evidence in the message, in the voice of the existing commits.
